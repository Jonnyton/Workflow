VERDICT: adapt

Required: omitted-universe routing still leaks another founder’s serial home outside `get_status`. `get_status` uses `_resolve_entry_universe()` to avoid `.active_universe` / first-dir fallback, but `universe action=inspect` and many other universe handlers still fall through `_default_universe()` at [helpers.py](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tinyassets/api/helpers.py:70) and [universe.py](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tinyassets/api/universe.py:145). I reproduced: founder A creates `u-*`; read-only founder B with no home gets `default-universe` from status, but `universe action=inspect` with no `universe_id` returns A’s `u-*`. This violates D10 / risk note “Remove `.active_universe` from MCP default routing” in [design.md](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/openspec/changes/universe-creation/design.md:199). Adaptation: introduce one shared MCP universe resolver for omitted `universe_id` and use it across universe/wiki/run/autoship universe-scoped defaults; add a regression test for non-status `universe` calls.

Required: OKF reserved files are still modeled as concept docs. [universe_bundle.py](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tinyassets/universe_bundle.py:4) says every seeded file has `type`; [index.md/log.md/soul_versions/index.md](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tinyassets/universe_bundle.py:333) are generated through `_doc()`, and tests enforce that at [test_universe_bundle.py](C:/Users/Jonathan/Projects/TinyAssets/.claude/worktrees/permissions-fail-closed/tests/test_universe_bundle.py:39). Current upstream OKF says `index.md` and `log.md` are reserved, non-reserved files require `type`, and root `index.md` only permits `okf_version` frontmatter. Source: https://raw.githubusercontent.com/GoogleCloudPlatform/knowledge-catalog/main/okf/SPEC.md. Adaptation: remove concept `type` frontmatter from reserved files, keep only allowed root `okf_version` if needed, and update tests.

Required before merge/live: branch is stale. `python scripts/session_sync_gate.py` reports this worktree is 3 commits behind `origin/main`. Rebase/merge current main, then rerun the focused auth/ACL/OKF suite.

Evidence: `git diff --check origin/main...HEAD` passed. `python -m pytest tests/test_first_contact.py tests/test_require_auth_challenge.py tests/test_workos_provider.py tests/test_universe_write_boundary.py tests/test_universe_server_isolation.py -q` passed: 121 tests. `tests/test_universe_bundle.py` passed but encodes the wrong OKF reserved-file expectation. I also rechecked WorkOS AuthKit MCP/token docs; `/oauth2/jwks`, Resource Indicator/audience, PRM, and AS metadata proxying align with https://workos.com/docs/authkit/mcp and https://workos.com/docs/authkit/connect/oauth.
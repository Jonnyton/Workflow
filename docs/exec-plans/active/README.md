# Active Execution Plans

Put one markdown file per active plan in this directory.

Use an execution plan when:

- the idea needs more than one meaningful implementation step
- verification has staged checkpoints
- multiple agents or sessions need the same delivery map

Retire completed plans into `../completed/`.

# Status

Live project state. Curated each session.

### How This File Works

Items flow: **ideas/INBOX.md -> ideas/PIPELINE.md -> Concerns / Work -> Watch -> Archive** (or AGENTS.md Hard Rules).

- **ideas/INBOX.md** is for fast capture of loose user ideas.
- **ideas/PIPELINE.md** is for triage, deduplication, and promotion.
- **Concerns** are raw capture. One line, instant, no analysis.
- **Work** is the ordered task board and dispatch surface. Position is priority. Each item has a **Files** column (what it writes — the collision boundary) and a **Depends** column (what must be done first, including read dependencies). **Status** tracks dispatch/landing only; it does not by itself mean "currently verified." Current trust lives in **Verify**. See AGENTS.md Parallel Dispatch for claiming rules.
- **Watch** holds items worth keeping visible after landing. Legacy rows without explicit freshness labels should be read as historical until refreshed.
- **Archive** is append-only history with explanations.
- **Verify / Trust labels** should start with one of: `current:`, `historical:`, `contradicted:`, or `unknown:`. If a claim depends on runtime or test evidence, include the date and environment.
- If a user idea is not being executed in the current turn, make sure it lands in `ideas/INBOX.md` or `ideas/PIPELINE.md` instead of only in chat.

PLAN.md changes require user approval. When behavior contradicts a PLAN.md assumption, add it here as a Concern - do not implement the conflicting approach.
When current evidence contradicts an older Verify or Watch claim, rewrite or downgrade the claim immediately instead of leaving stale certainty in place.

Nothing is ever lost. Nothing is ever deleted.

---

## Concerns

- [2026-04-12] **Strategic pivot confirmed by user — community-branch authoring is the product trunk** -> user clarified late 2026-04-12 that Workflow's real product is "any user evolves the LangGraph via natural chat with claude.ai". Fantasy writing is one benchmark branch, not the trunk. Users should be able to build workflows of any topic with any state architecture. Two audiences: (1) non-technical — talks to chatbot in natural language about a goal, chatbot routes to tools that build the workflow; (2) nerdy — direct state/schema/node/edge control. Local-LLM daemon output quality limitations are acceptable and NOT the focus of near-term testing. Community Branches Phase 2 (graph design + state design MCP tools) is now the central near-term Work item. Planner scoping 2026-04-12; dev sequences after the telemetry/ledger fix cluster.
- [2026-04-13] **Phase 5 Goals landed — legion-per-Goal primitive is live** -> dev-2 shipped #54 (Phase 5 executable spec). New `goals` MCP tool with 8 actions: propose / update / bind / list / get / search / leaderboard / common_nodes. `goal_id` now a nullable column on branch_definitions. Composite build_branch/patch_branch honor goal_id. Leaderboard serves `run_count` + `forks` live; `outcome` returns Phase 6 forward-compat stub. Soft-delete via visibility=deleted. Free binding (host's Q2 call). Ownership: author-owned update, public bind, no approval gate. 37 tests. Reviewer + tester green. Live on Universe Server PID 19316. With this ship, the entire community-branches roadmap through Phase 5 is shipped in one session (Phase 2→3→3.5→composite→update_node→4→rollback→5). Mission 5 (reuse-vs-invent across branches under same Goal) is now fully surface-ready and can include Goal-aware probes.
- [2026-04-13] **Phase 2→3→3.5→4 all live on the MCP surface** -> Universe Server restart on PID 25256 after dev's #46 merge confirmed 29 actions on the `extensions` tool: build_branch, patch_branch, create_branch, get_branch, list_branches, delete_branch, add_node, connect_nodes, set_entry_point, add_state_field, validate_branch, describe_branch, update_node, register, list, approve, run_branch, get_run, list_runs, cancel_run, stream_run, get_run_output, judge_run, list_judgments, compare_runs, suggest_node_edit, get_node_output, rollback_node, list_node_versions. Plus affirmative-consent routing (#46), NO SIMULATION clause (#34), async execution (#39), mermaid auto-rendering (#32), phone-legible return shapes, ledger write-through (#11). Entire community-branches roadmap Phases 2-4 shipped in one session by three devs working in parallel. Mission 5 (reuse-vs-invent same-Goal) and Mission 6 (prosecutorial-brief new-Goal) are fully surface-ready.
- [2026-04-13] **Vision completed: outcome-gate ladders make real-world impact the truth signal** -> Host clarified 2026-04-13: every Goal has a ladder of real-world outcome gates beyond the workflow's immediate output. Research papers → peer-reviewed publication + citations + breakthrough recognition. Fantasy novels → published, bestseller, awards. Legal briefs → prosecutor engagement → filing → conviction. Each has many sub-steps trackable over time. PLAN.md updated with "Outcome gates — real-world impact is the truth signal" subsection. Evolutionary signal amplifies: fork the branch whose outputs ACTUALLY reached g4, not the branch with good chat-side judgment. Leaderboards rank on outcome progression. Tracking is self-report + manual at first; automation (DOI, court-docket, sales, awards registries) comes later as integrations prove value. Reframes the project: Workflow is infrastructure for collective pursuit of real-world outcomes, AI workflows are one leverage layer. Captured as Phase 6+ direction; not blocking near-term missions but shapes every product decision.
- [2026-04-13] **Mission 6 queued: prosecutorial-brief Goal — evidence-heavy legal synthesis stress test** -> Host directive 2026-04-13: next user-sim mission builds a workflow on a new Goal (`prosecutorial-brief`) against public Epstein files + public internet. Framing: high-quality synthesis drafts lawyers would refine, NOT filable court documents. Hard invariants in the workflow: every factual allegation MUST cite a public-record document, no allegations against non-publicly-named individuals, output labeled "synthesis draft, requires licensed review." This is the first legal-domain Branch and tests our architecture's evidence-grounding + citation-chain handling. Mission plan at `output/mcp_test_plan.md`. Gated on #46 + Phase 4 + rollback (#31/#50). Output quality with local LLM is NOT the test — architecture is.
- [2026-04-13] **Vision sharpened: planetary-scale collective pursuit per shared Goal** -> Host clarified 2026-04-13 the multi-user vision: broad Goals (research papers, fantasy writing, etc.) are first-class shared pursuits the entire world open-source-collaboratively pursues SIMULTANEOUSLY. Each Goal accretes a legion of diverse Branches — different users' takes on that same ultimate outcome. Diversity is a feature, not duplication. PLAN.md updated: Goal is elevated above Branch as a first-class object; "invent parallel to fork" is the canonical mode (users aren't just improving one canonical branch, they're inventing their own); Goals are extensible (users propose new Goals); leaderboards per Goal emerge from run data + judgments; cross-branch node library per Goal surfaces patterns that work. This reshapes Phase 5+ scope — Goal storage + per-Goal discovery/leaderboards are now core, not follow-up.
- [2026-04-13] **Multi-user evolutionary design thesis added to PLAN.md** -> Host clarified 2026-04-13: long-term vision is 100 users over months all building their own variants of the same goal (e.g. research-paper workflows), all branches shared and public by default, users learn from each other. Phase 5+ direction captured under new PLAN.md "Multi-User Evolutionary Design" section. Required surfaces not yet built: node identity across branches, fork as first-class action, search/filter over public corpus, social judgment signals, authorship/attribution chains. Mission 5 (reuse vs invent a SECOND research-paper workflow) is the first observation-collecting run that shapes Phase 5 specs.
- [2026-04-13] **Mission 4 GREEN — cross-session workflow persistence proven end-to-end** -> user-sim opened a fresh claude.ai chat with zero context, anchored to Universe Server, found the 7-node research-paper workflow built in a prior session (extensions list_branches + get_branch), compiled it via Phase 3 runner, invoked `run_branch` (no simulation — #34 holding), and honestly reported prompt-template substitution failure (#44) when outputs came back broken. Core architecture thesis validated: workflows are durable, reusable, shareable artifacts across sessions, not one-chat throwaways. Every test burned bugs that were already in flight — the ship loop is working. Mission 5 (eval + iteration: judge output → edit node → rerun → compare) unlocks after Phase 4 (#31) scopes + ships.
- [2026-04-13] **Test-domain gating rule: complex-output workflows only** -> Host clarified 2026-04-13 that Workflow's test domains must be multi-step, stateful, memory-heavy, evaluation-bound, and produce substantive output — a paper, a book, a screenplay, a meta-analysis, an investigative series. NOT list/tracker tasks (wedding planning, recipes, weekly summaries) that a plain chatbot already handles — those don't stress the architecture. Codified in `.agents/skills/ui-test/SKILL.md` and `output/mcp_test_plan.md`. Mission 2's wedding/recipe domains were miscast; the research-paper domain the host asked for is correct. All future user-sim missions select domains that justify the architecture's complexity, or stop and ask the lead.
- [2026-04-13] **Ledger write-wrapper landed (bug #11 resolved)** -> dev refactored `workflow/universe_server.py` with a shared WRITE_ACTIONS-gated write-wrapper at the dispatch site; no handler-side ledger code; exhaustiveness test guards future drift. 19 tests green, ruff clean, FastMCP end-to-end smoke confirmed. Reviewer approved. Unblocks task #19 (Community Branches Phase 2) — every Phase 2 write will get ledger attribution for free. Dev now on Cluster B (telemetry cluster #8/#10/#14/#16/#17 + #18 query routing) as one coherent landing.
- [2026-04-12] **12 bugs surfaced in one hour via browser-driven user-sim testing** -> user-sim (simulated Claude.ai phone user, driving a visible Chrome tab via `scripts/claude_chat.py`) produced tasks #8-#18 in ~1h: systemic ledger bypass on all user writes (#11), telemetry illegibility cluster (#8/#9/#10/#14/#16/#17 — phase/liveness/premise-missing/accept_rate never surfaced truthfully), data drift (#12 word_count, #13 premise escapes, #18 sporemarch DB schema), and claude.ai-side cross-universe hallucination + false-liveness reasoning (#15/#16). Two testing architectures proven this session: (a) direct-MCP probing via `scripts/mcp_call.py` (retired — too invisible, user couldn't watch) and (b) browser-driven via CDP at port 9222 with a dedicated Chrome profile (`~/.claude-ai-profile`) — which caught a whole second class of chat-side UX bugs the direct path never could. Browser path is now the canonical user-sim surface; skill at `.agents/skills/ui-test/` mirrors to .claude/.codex.
- [2026-04-12] **`default-universe` daemon stuck in worldbuild no-op loop since 2026-04-09** -> live inspection via phone/Claude.ai on 2026-04-12 found the `default-universe` daemon cycling worldbuild → "No changes" → worldbuild repeatedly, version 12 → 14, phase "unknown", accept rate 0.0%. Last logged activity ~5:49 AM on 2026-04-09 (3 days of no-op cycles). Universe has 53,543 words but no premise set. Likely interaction of (i) no premise / no active work targets giving the daemon nothing to do, (ii) bounded reflection / loop guardrails failing to catch the no-op pattern and escalate or self-pause. Needs investigation — this is exactly the scenario bounded reflection in `orient` is supposed to detect. See task #6.
- [2026-04-12] ~~**Near-term session goal: end-user reachability test — phone → Claude.ai → Universe Server MCP at `https://tinyassets.io/mcp`**~~ **RESOLVED 2026-04-12.** Live phone → Claude.ai → `tinyassets.io/mcp` test succeeded. Tunnel returned 200 OK on MCP `initialize` (session ID assigned, protocolVersion 2025-06-18, serverInfo "universe-server v0.1.0", tools/prompts/resources declared, control-station instructions delivered). User confirmed from their phone that the connector persists across sessions, authless dev-mode is accepted (blocker b cleared — no OAuth required), and tools fire correctly against live data (universe list returned 4 universes with word counts + premise state). Blocker (a) cleared earlier same day by dev (`workflow.universe_server` imports cleanly after Phase 2b). Blocker (c) audited — titles/hooks/annotations pass; docstring trim deferred as Work item (not required for probe). Stable URL `https://tinyassets.io/mcp` (Cloudflare tunnel `b59f3cd9-a47a-4c97-80e4-7826513de630` → localhost:8001) proven as a durable public end-user interface.
- [2026-04-10] **CRITICAL: Sporemarch Book 1 total premise departure + cross-universe contamination** -> Critic review of all 30 scenes (Ch1-Ch10, ~72K words) found: (1) protagonist Loral Duskspore absent from all scenes, replaced by Caro/Corin dual POV; (2) entire PROGRAM.md world system (Underhallow, germination, Mycelia, Mycoturgy, all factions) absent — zero occurrences; (3) Ashwater universe characters/locations leaked into Sporemarch (Durnhollow, Vael Reach, Wren, Daeren, Torben — 106 occurrences across 16 files); (4) character identity breaks: Wren→Ryn name swap at Ch5, Ryn gender oscillation (she in 5 scenes, he in 3), Daeren→Kael swap at Ch8, dog Ashka vanishes at Ch6; (5) Ch7 S2 severe degradation with duplicated paragraphs and incoherent worldbuilding; (6) evaluator accepted all 30 scenes (scores 0.63-0.84) catching none of this. Root cause unknown — needs investigation: KG contamination, context assembly failure, or evaluator calibration gap. This directly validates the need for structured scene artifacts with premise-grounding validation gates.
- [2026-04-10] ~~**Evaluator is blind to premise departure and cross-universe contamination**~~ -> resolved 2026-04-11. Premise grounding added to structural evaluator (deterministic zero-LLM check: protagonist presence, world-term grounding, cross-universe contamination detection) plus editorial reader now receives premise context. Hard failure on zero world terms or missing protagonist. Tests reproduce exact Sporemarch failure. Reviewer approved, tester confirms no regressions.
- [2026-04-09] **URGENT REFACTOR RESEARCH** BettaFish deep dive complete -> `BettaFish_Deep_Dive_Research.docx` in project root. 40,300-star multi-agent system validates Workflow's file-based shared state and framework-minimal approach. Six transferable patterns identified with full implementation specs: (1) ForumEngine structured agent debate, (2) Document IR for output pipeline, (3) semantic result clustering for retrieval, (4) keyword optimization middleware, (5) multi-model evaluation stack, (6) bounded reflection loops. Phased adoption roadmap: Phase 1 immediate (clustering, reflection, json-repair, tenacity), Phase 2 next sprint (debate engine, Document IR, keyword optimizer, Pydantic Settings), Phase 3 following sprint (multi-model eval, process lifecycle). All providers should review before next sprint planning. This is the reference architecture research for taking Workflow to frontier status.
- [2026-04-08] Official MCPB docs and validator are not fully in sync -> current public `MANIFEST.md` text mentions `server.type = "uv"`, but the official `@anthropic-ai/mcpb` validator on 2026-04-08 rejects that enum and accepts only `python | node | binary`. The shipping bundle therefore uses `server.type = "python"` with `mcp_config.command = "uv"` as a compatibility workaround while still staging a minimal FastMCP runtime.
- [2026-04-08] MCP registry publishing is still blocked on a public distribution URL -> the Claude plugin marketplace surface has been rewritten and now validates natively, and `packaging/registry/server.json` now passes the official schema. The remaining blocker is publication, not structure: the registry draft currently points at a planned GitHub Releases URL for `workflow-universe-server-0.1.0.mcpb`, but that release asset has not been uploaded publicly yet.
- [2026-04-08] `docs/planning_sweep_2026-04-08.md` remains the current planning-sweep reference in `STATUS.md`, but the user explicitly paused any follow-up work on it for tonight -> keep it visible as a handoff/reference artifact, not an active workstream until resumed in a later session.
- [2026-04-08] Long-term project direction is now clarified by user -> Workflow is the primary identity: a general agent-workflow engine, host-run multiplayer daemon platform, and long-horizon agent lab. Fantasy Author is an early benchmark branch, not the trunk. The destination is multi-host, domain-agnostic, self-evolving, conversationally steerable workflow/state design with real-world tracked outcomes.
- [2026-04-08] ~~Project-level "Author" terminology is now strategically misaligned~~ -> resolved 2026-04-11. Full identity alignment sweep: AGENTS.md, LAUNCH_PROMPT.md, all `.claude/agents/*.md`, pyproject.toml, custom_gpt/, .mcp.json, desktop UI strings, and historical docs all reframed from "Fantasy Author" to "Workflow" with daemon/summoning language. Python imports migrated to `workflow.*` / `domains.fantasy_author.*`.
- [2026-04-08] Conway readiness should be treated as packaging/distribution work, not a core-architecture fork -> the durable strategy is MCP-first portability plus Anthropic-surface packaging (`mcpb`, Claude plugins, future Conway wrapper) so the project is discoverable on launch day without locking core runtime behavior to speculative Conway-only assumptions.
- [2026-04-08] User approved the Codex long-text mitigation path -> implement a repo-local large-doc query surface and update `AGENTS.md` so agents use it instead of raw whole-file reads for `PLAN.md`, `notes.json`, and other oversized text artifacts.
- [2026-04-08] Codex long-text file handling needs an explicit safe path -> prior truncation already damaged source files once, and the user now wants a repo-level mitigation that gives agents a non-truncating surface for large text plus clear `AGENTS.md` guidance on when to use it.
- [2026-04-08] Repo-scale docs already exceed reported Codex long-file thresholds -> local audit shows `PLAN.md` is ~53.6 KB / 967 lines and `output/sporemarch/notes.json` is ~219.9 KB, while recent official `openai/codex` issue reports describe truncation around `10 KiB` / `200 lines` for some local-file and tool-output paths. Large-file handling should therefore be explicit, not left to ad hoc raw reads.
- [2026-04-08] `PLAN.md` revalidation found mixed trust by section -> thesis-level sections still mostly hold, but several operational sections are better described as bridged, and `Workflow Extraction` is overstated in present tense because structural extraction landed while `workflow` runtime/API still bridge through `fantasy_author`. See `docs/plan_revalidation.md`.
- [2026-04-08] Source-of-truth trust is now itself in question -> `PLAN.md` was intended to be authoritative design intent, but drift/misuse may have made some plan claims lower-confidence than code, runtime artifacts, or surviving project evidence. The next audit should not assume any single file is canon by assertion; it should reconstruct truth from evidence tiers and explicitly label confidence.
- [2026-04-08] Shared-state docs have lost trust as reliable truth surfaces -> `STATUS.md` and `PLAN.md` were intended to prevent drift, but they accumulated stale claims because there is no explicit freshness/verification mechanism that continuously compares documented state against the actual repo, runtime, and environment. A repo-vs-doc audit/reporting workflow is needed without creating a hidden fourth source of truth.
- [2026-04-08] Live repo state has moved past "first smoke test pending" -> `output/.active_universe` is `sporemarch`, `output/.author_server.db` is active, and `logs/daemon.log`, `logs/mcp_server.log`, and `logs/tunnel.log` were all written on 2026-04-08. The daemon and Universe Server are being exercised live, not just prepared offline.
- [2026-04-11] Verification baseline refreshed -> `ruff check`: 0 errors. `pytest`: 1799 passed, 36 failed (all pre-existing), 1 skipped. Session gained +184 passing tests (universe isolation 20, premise grounding 12, scene packets 3+13, bounded reflection 22+35, community branches Phase 1 93, wiki tools, evaluation suite growth). Zero new regressions. Pre-existing failures: desktop/tray (12), context compaction Windows PermissionError (6), phase7/integration (5), API/schema drift (4), context guardrails (3), author server (2), GPT contract (2), import compat (1), research probe (1).
- [2026-04-08] ~~Workflow extraction is structurally present but still operationally bridged~~ -> updated 2026-04-11. Phase 2b import migration complete: all 623 test/script imports migrated from `fantasy_author.*` to `workflow.*` / `domains.fantasy_author.*`. `fantasy_author/` converted to thin re-export shims (originals backed up in `fantasy_author_original/`). Entry points renamed to `workflow-*`. Env vars renamed to `WORKFLOW_*`. Runtime still bridges through `fantasy_author/__main__.py` and `fantasy_author/api.py` (Phase 5 bridge — these two files kept as real code to avoid circular imports). Phase 3 will remove shims entirely.
- [2026-04-08] Live daemon logs show active runtime pressure -> recent `sporemarch` runs hit provider exhaustion for `writer` and `extract`, empty draft output, Ollama embedding 404s during indexing, and a `Context bundle exceeds budget` warning at ~78K tokens against a 15K budget. This validates older "keep watching legacy context assembly" concerns with current runtime evidence.
- [2026-04-06] Universe Server MCP built (`universe_server.py`) -> the primary public interface. Streamable HTTP transport, coarse-grained tools, MCP prompts for behavioral instructions. Any MCP-compatible chatbot connects with just a URL. Tunnel stays for dev; stable URL at alpha. (Custom GPT is now legacy.)
- [2026-04-06] User node registration system added -> users can register custom LangGraph nodes via MCP. Nodes are sandboxed, host-approved, and wired into the graph at declared phases. The graph evolves through community contribution, not just developer iteration.
- [2026-04-06] Evolving graph thesis -> the node registration + evaluation system should enable the LangGraph to learn over time. Nodes that improve outcomes (measured by existing eval layer) get promoted; weak nodes get pruned. The workflow itself becomes a living artifact that self-improves through collective use.
- [2026-04-06] MCP prompts cannot inject system prompts automatically -> MCP spec treats prompts as user-invokable templates, not automatic system context. The `control_station` prompt must be explicitly invoked by the connecting chatbot. This is a spec limitation, not a bug — workaround is rich tool descriptions and well-named prompts.
- [2026-04-06] OAuth 2.1 + Dynamic Client Registration needed for production auth -> MCP spec uses OAuth 2.1 + PKCE + RFC 7591 DCR so any MCP client can self-register. Dev mode is authless. Production needs `.well-known/oauth-authorization-server` and `.well-known/mcp-resource` endpoints.
- [2026-04-06] Official MCP registry exists at registry.modelcontextprotocol.io -> listing the Universe Server there makes it discoverable by any MCP client. Submit via GitHub PR when ready for alpha.
- [2026-04-05] Workflow Extraction Phases 0-2 complete. Infrastructure modules copied to workflow/; domain structure created in domains/fantasy_author/. Phases 3-5 (context/memory upgrades, generality probe, full end-to-end run) in progress. D5 open questions (package name, monorepo, config loading, multi-skill) still have only Claude positions — Codex positions pending.
- [2026-04-05] Claude populated the debate surface in `PLAN.md`; user now wants Codex responses written in-place -> fill the open Position B blocks, add Codex-labeled rebuttals to Claude's rebuttals, and keep future Codex debate text explicitly tagged `[Codex]`.
- [2026-04-05] User wants the live PLAN debate area to use explicit provider tags -> ongoing architecture arguments in `PLAN.md` should be written under clearly labeled provider blocks such as `[Codex]` and `[Claude]`.
- [2026-04-05] User redirected the live restructure debate from `RESTRUCTURE_PLAN.md` into `PLAN.md` -> future Claude/Codex argument, research synthesis, and proposed architecture rewrites should land in `PLAN.md`, with `RESTRUCTURE_PLAN.md` treated as scratch/deprecated.
- [2026-04-05] User wants `RESTRUCTURE_PLAN.md` to become the live cross-provider debate surface with Claude/Codex iterations -> proposed restructures should be written into that draft directly for rebuttal/refinement rather than left only in session chat.
- [2026-04-05] Only one RESTRUCTURE_PLAN debate point has been applied so far -> the draft now drops the fixed token-cap claim, but the broader proposed rewrites (phase ordering, contract-first migration, skills-vs-subgraphs framing, scoped memory emphasis, extraction timing) are still only in analysis and not yet written into `RESTRUCTURE_PLAN.md` for cross-provider debate.
- [2026-04-05] User resolved one RESTRUCTURE_PLAN debate point -> context/tool compaction should stay bounded and paginated, but not by a hard architectural token cap; the draft should avoid presenting `~25K tokens` as settled guidance.
- [2026-04-05] User wants a deeper big-picture read before test/iteration work -> the next pass should focus less on file inventory and more on project thesis, intended end-state behavior, agent-workflow principles, and what Fantasy Author is trying to prove beyond the current implementation seams.
- [2026-04-05] Deep-dive found the work-target scheduler is still source-broken -> `graphs/universe.py` and the review/dispatch nodes import `fantasy_author.work_targets`, but only `work_targets.py.truncated` is present in the package. The runtime is therefore depending on stale bytecode / prior state instead of current source, and the work-target API/tests cannot be trusted until the module is restored.
- [2026-04-05] Deep-dive found notes/API/schema drift -> `notes.py` still exposes the old minimal note model (no `tags`, no `metadata`, no `get_active_direction_notes`), while `commit.py`, `worldbuild.py`, `work_targets.py.truncated`, the MCP/API schema, and work-target tests all expect the richer notes surface.
- [2026-04-05] Deep-dive found multiplayer substrate is implemented below the HTTP layer but not wired through it -> `author_server.py` now contains sessions/authors/branches/runtime/ledger primitives, but `api.py` remains the older file-adapter surface, so the new multiplayer tests and MCP/API schema reference endpoints the FastAPI app does not yet expose.
- [2026-04-05] Deep-dive confirmed the desktop host split is still incomplete -> `host_tray.py` and desktop tests expect a richer shared-tray API (`refresh_menu`, extra menu injection, hidden default controls, launcher binding through `HostTrayService.shared()`), while the reverted `tray.py`/`launcher.py` still use the older single-runtime tray path.
- [2026-04-05] Deep-dive found steering-era residue still on the runtime startup path -> `__main__.py` still reads `STEERING.md` into `workflow_instructions` even though notes are supposed to be the only feedback path. This is at least compatibility residue and may still shape live runs if a steering file is present.
- [2026-04-05] Codex output truncation damaged 8 files -> reverted 7 tracked files (`api.py`, `notes.py`, `__main__.py`, `launcher.py`, `tray.py`, `desktop/__init__.py`, `nodes/__init__.py`) to HEAD. `work_targets.py` (new, 764 lines) also truncated and set aside. All complete new files committed. Codex needs to redo modifications to those 8 files in a fresh session with parallel dispatch protocol.
- [2026-04-05] ~~Desktop host UX is leaking duplicate detached UI/runtime objects~~ -> repaired offline for the launcher path: one shared host tray now aggregates live universe dashboards, and duplicate tray/daemon/poller starts are guarded. Keep watching cross-process desktop launches until the first real host run proves the tray no longer multiplies.
- [2026-04-05] Shared chat interface should not become the host operations console -> every universe with a live daemon needs a locally run host dashboard with a live feed and host/admin controls, while normal users continue using the MCP/chat surface.
- [2026-04-05] Initial multi-Author test order is now explicit -> first live Author stack should be Claude Opus 4.6 as the default lead Author, then local/Ollama as the cheap always-on second Author, then Codex, then additional providers after the first round proves stable.
- [2026-04-05] Near-term multiplayer goal is now concrete -> user wants to test multiple Authors soon on one host using the existing local + Codex + free-tier/X-subscribed provider mix, so the implementation path should optimize for an early 2-3 Author live test before a full distributed scheduler exists.
- [2026-04-04] Multiplayer Author-server substrate is now SQLite-backed and transactional for V1, but SQLite remains a single-host/single-writer ceiling -> make the Postgres/external-store migration path explicit before treating multiplayer as a durable scale architecture.
- [2026-04-04] Runtime registry and governance surface now exist, but actual swarm scheduling does not -> multi-runtime branch/work-target claiming still needs an external claim/lease queue or equivalent scheduler design beyond LangGraph handoffs.
- [2026-04-04] Stale steering/directive references still survive in archived docs, old worktrees, caches, and auxiliary prompts -> runtime/API/MCP are still notes-first, but these artifacts can create false regression reports during reviews.
- [2026-04-04] Approved architecture now exceeds the current single-daemon API model -> the repo needs a real multiplayer Author-server substrate: named sessions, public action ledger, branch-first collaboration, Author identity/forking, and SQLite-backed contested state.
- [2026-04-04] Concurrent MCP/API users currently share one daemon and shared JSON/file artifacts with no per-user isolation or cross-request write locking -> read-heavy use is mostly fine, but concurrent notes/canon/premise/daemon-control actions can race, lose updates, or globally switch/pause the daemon for everyone.
- [2026-04-04] ~~Daemon hold -> do not run the daemon until the offline architecture/code update pass is complete; prefer planning, refactors, and verification that do not require a live writer.~~ -> offline bridge work is complete; repo-wide `ruff check` and full `pytest` now pass, so the next step can be a controlled first live daemon smoke test.
- [2026-04-04] Runtime readiness narrowed after local verification -> startup preflight + inspectable target-registry bootstrap are repaired offline; actual Ashwater runtime KG is populated (449 entities / 617 edges / 220 facts / 256 communities), so the next real unknown is the first supervised live run under the new review/execution flow.
- [2026-04-04] ~~Queue-first universe scheduler no longer matches the approved architecture~~ -> repaired offline: universe graph now runs foundation review -> authorial review -> dispatch -> execution -> cycle, with `task_queue` retained only as a compatibility mirror while lower loops migrate.
- [2026-04-04] ~~Work classification needs a guarded layer above free tags~~ -> repaired offline: `WorkTarget` now carries guarded role/publish-stage/lifecycle fields above free tags, including provisional publishability and delayed true-discard rules with tests.
- [2026-04-04] ~~Selected work targets were not yet controlling lower-loop execution scope~~ -> repaired offline: target metadata now seeds book/chapter/scene execution, resumes open chapters from output artifacts, carries prior-scene continuity forward, and preserves the no-target compatibility path for legacy callers.
- [2026-04-04] Project framing widening beyond fantasy output -> treat Fantasy Author as a hard testbed for general principles of intelligent iterative agent workflows; distill durable principles from repo + frontier research without overfitting to current architecture.
- [2026-04-04] ~~Scene loop still pre-assembles canon, retrieval, memory, and notes context despite PLAN.md target of tool-driven context.~~ -> repaired for the main writer path: plan/draft now query explicit writer tools and orient no longer consumes notes; legacy fallback/context assembly still remains outside the primary writer loop.
- [2026-04-04] Retrieval and memory were split into separate phase bundles even after the tool-surface rewrite. -> repaired for the primary writer loop: orient/plan/draft now build a shared `search_context`, and `story_search` merges routed retrieval, memory, notes, canon, and world-state into one writer-facing tool. Keep watching legacy consumers that still read raw `retrieved_context` / `memory_context` directly.
- [2026-04-04] ~~State contract drift between TypedDicts, orient/commit outputs, and memory consumers risks long-run brittleness.~~ -> repaired in scene bootstrap, orient_result, retrieval lifecycle, and extracted fact serialization; keep watching for regressions under future refactors.
- [2026-04-04] ~~Steering residue still survives in commit/editorial path despite unified notes replacing steering as the feedback system.~~ -> repaired: commit/editorial/worldbuild now read active direction notes, MCP/API surfaces route through notes, and steering-era runtime fields are gone.
- [2026-04-03] Ashwater scenes 01-03 broken -> stress test for self-correction auditor. Prove auditor works on clean universe first, then throw it at Ashwater.
- [2026-04-03] ~~Tunnel URL ephemeral~~ -> contradicted 2026-04-12. Stable named Cloudflare tunnel bound to `tinyassets.io` (user-owned via GoDaddy) now exists; ingress config `tinyassets.io → http://localhost:8001`, tunnel ID `b59f3cd9-a47a-4c97-80e4-7826513de630`, token embedded in `universe_tray.py`. The public URL is durable; only the local host-side processes are on/off. Rate-limit sharing between dev and daemon still applies.
- [2026-04-03] User agent real-world despawn -> untested until next MCP client session.
- [2026-04-04] Upload provenance tags -> the client tags each upload with ~50 char description (e.g. "published book", "rough notes"). Writer uses tags to weight how seriously to treat each canon source.
- [2026-04-04] Universe = single consistent reality -> time periods within same universe share timeline. Alternative realities = separate universes with divergent facts.
- [2026-04-04] Graph hierarchy (universe/book/chapter/scene) is scaffolding -> internal structure should emerge from writer's choices, not be enforced by counters. Only hard boundary is data isolation between universes.
- [2026-04-09] Runtime fiction memory is still artifact-rich but semantically under-modeled -> canon markdown, scene prose, and note streams exist, but the daemon still needs a stronger typed world/event/epistemic/narrative-debt memory substrate to stay coherent across long-running fantasy. See `docs/design-notes/2026-04-09-runtime-fiction-memory-graph.md` and `docs/exec-plans/active/2026-04-09-runtime-fiction-memory-graph.md`.
- [2026-04-09] Research brief completed for runtime fiction memory graph -> frontier research (DOME, SCORE, StoryWriter, A-Mem, LightRAG, MemOS) validates the four-layer decomposition and identifies concrete gaps: scene commit packets, narrative debt scoring, incremental graph union, epistemic per-character state, typed retrieval routing, and next-scene context pre-staging. Existing HippoRAG/RAPTOR/Leiden/phase-routing stack is already near-frontier; the missing layer is structured state accumulation, not more retrieval backends. See `docs/design-notes/2026-04-09-memory-graph-research-brief.md`.
- [2026-04-03] ~~Entity extraction 0 entities~~ -> resolved. KG has 449 entities. Scene->KG integration test written.
- [2026-04-04] ~~Regex fallback quality~~ -> reframed: synthesis now uses writer role, no truncation, verification pass.
- [2026-04-03] ~~Judge calibration~~ -> resolved. Old 3-tier scoring pipeline replaced with editorial feedback model. Anchor-passage module deleted.
- [2026-04-03] ~~Graph flattening~~ -> resolved by daemon-driven principle. Model judges structural boundaries.
- [2026-04-03] ~~Evaluator untested~~ -> evaluator completely redesigned (editorial model). Production test needed after daemon runs on new system.

## Work

Ordered by importance - position is priority. Reordered each session.

### Session 2026-04-13 landings (newest at top)

Community-branches Phases 2→3→3.5→4 plus rollback and composite + all intent-routing + telemetry + cross-universe cluster fixes shipped in this session. Live tool surface has 29 actions on `extensions`. Universe Server restarted many times; currently running on auto-restart via tray.

| Task | Files | Depends | Status | Verify |
|------|-------|---------|--------|--------|
| **Community Branches Phase 2 — builder** | `workflow/universe_server.py`, `fantasy_author/branches.py`, `tests/test_community_branches_phase2.py` | Phase 1 | done | current: 2026-04-13. 10 actions (create_branch, get_branch, list_branches, delete_branch, add_node, connect_nodes, set_entry_point, add_state_field, validate_branch, describe_branch). Mermaid in describe_branch per #32. 18 tests green. Reviewer approved. |
| **Composite build_branch + patch_branch (#43)** | `workflow/universe_server.py`, `tests/test_composite_branch_actions.py` | Phase 2 | done | current: 2026-04-13. Strict-with-suggestions validation. Transactional patches. One ledger entry per composite. 25 tests. Reviewer approved. Live. |
| **update_node action (#45)** | `workflow/universe_server.py`, `tests/test_composite_branch_actions.py` | Phase 2 | done | current: 2026-04-13. Stable node_id across edits, version_before/after returned, prompt↔source mutual exclusion. 16 tests. Live. |
| **Phase 3 runner + async (#30 + #39)** | new `workflow/graph_compiler.py`, new `workflow/runs.py`, `workflow/universe_server.py`, `tests/test_community_branches_phase3.py` | Phase 2 | done | current: 2026-04-13. BranchDefinition→StateGraph compiler. SqliteSaver at `output/.langgraph_runs.db`. Sync v1 then async runner shipped same session. 6 actions: run_branch, get_run, list_runs, cancel_run, stream_run, get_run_output. Status-colored mermaid. Cancel actually interrupts between nodes. Restart recovery marks in-flight as interrupted. 40 tests. Live. |
| **Prompt-template substitution fix (#44)** | `workflow/graph_compiler.py`, `tests/test_community_branches_phase3.py` | Phase 3 | done | current: 2026-04-13. `{{var}}` Jinja-style now normalizes to `{var}` before str.format_map. Invariant: un-substituted placeholders raise, never leak to LLM. 8 tests. Live. |
| **Phase 4 eval + iteration + rollback (#31 + #50)** | `workflow/universe_server.py`, `workflow/runs.py`, `tests/test_community_branches_phase4.py` | Phase 3, update_node | done | current: 2026-04-13. 7 actions: judge_run, list_judgments, compare_runs, suggest_node_edit, get_node_output, rollback_node, list_node_versions. New tables: run_judgments, run_lineage, node_edit_audit with full pre/post body snapshots. Judgments free-text no rubric. Rollback preserves forward history. 44 tests. Live. |
| **Ledger write-wrapper (#11)** | `workflow/universe_server.py`, `tests/test_universe_server_*` | - | done | current: 2026-04-13. Dispatch-site WRITE_ACTIONS wrapper; no per-handler ledger code; exhaustiveness test guards drift. Every write action ledgers with author+action+target+payload_summary. 19 tests. Live. |
| **Telemetry legibility cluster (#8/#10/#14/#16/#17/#18)** | `workflow/universe_server.py` | - | done | current: 2026-04-13. inspect returns has_premise/last_activity_at/staleness/phase_human. control_daemon status matches. accept_rate wired to scene_history (real commit count). _query_world_db routes characters→knowledge.db::entities / facts→story.db::extracted_facts / promises→story.db::promises. 40 tests. Live. |
| **Commit pipeline write-back regression (#22)** | `domains/fantasy_author/phases/commit.py` | - | done | current: 2026-04-13. Two bugs: (1) record_scene called with verdict=pending BEFORE commit judged; split to `_record_scene_verdict` called after. (2) `_init_retrieval_backends` didn't resolve `_universe_path` absolute — KG writes landed in CWD. Both fixed. 81 tests. Live smoke confirmed via sporemarch restart. |
| **Affirmative-consent intent routing (#46)** | `workflow/universe_server.py`, `tests/test_universe_server_framing.py` | - | done | current: 2026-04-13. `register` + `build_branch` tool descriptions prepend: only invoke when user explicitly asks to create/build; query-intent phrases route to list instead; on ambiguity, ASK. `control_station` prompt adds Intent disambiguation section. 4 tests. Live. |
| **MCP connector discoverability by natural name (#25)** | `workflow/universe_server.py`, `packaging/registry/server.json` | - | done | current: 2026-04-13. Server instructions include: also known as workflow, workflow builder, workflow engine, custom AI builder, universe builder. Tool descriptions sprinkle natural-language synonyms. Registry description includes workflow-builder phrases. |
| **Cross-universe cluster — complete** | quarantine dir, `workflow/retrieval/vector_store.py`, `workflow/mcp_server.py`, `workflow/runtime.py` | - | done | current: 2026-04-13. On-disk: default-universe/canon/ 16 Ashwater files quarantined to `archive/stale-cross-universe-content/default-universe-canon-20260413/`. test-universe + upload-test-serve audited clean. Live code: retrieval/KG/memory paths enforce universe isolation; VectorStore + KG + WORKFLOW_UNIVERSE now hard-refuse empty/cwd-relative. runtime.reset() invariant documented. 48 audit complete. Only remaining surface: chat-side tool-description nudges for `universe_id` binding (task #15). |
| **Misc infra cluster** | various | - | done | current: 2026-04-13. Auto-dismiss dialog handler in claude_chat.py (#40). Rich-content artifact+mermaid capture in chat trace (#33). Live browser driver auto-launches Chrome if CDP not up (earlier in session). Encoding cp1252 crash fixed on ⇒ / em-dash. utf-8 forced on stdout/stderr. Dialog + input-lock recovery landed incrementally. |

### Claimable pending work (Files column is the collision boundary)

If you pick up a task, update Status to `claimed:yourname`. File column tells you what's exclusive.

| Task | Files | Depends | Status | Verify |
|------|-------|---------|--------|--------|
| **#15 chat-side cross-universe disambiguation** | `workflow/universe_server.py` (tool action descriptions + response-shape text) | #46 done | claimed:dev-3 (2026-04-13) | Tool responses about a universe need to lead with `Universe: <id>` so downstream reasoning can't lose track. Description nudges: don't transfer info across universes. |
| **#41 claude_chat.py input-locked recovery** | `scripts/claude_chat.py` | - | claimed:dev (2026-04-13) | Recovery ladder: click body → Escape → scroll bottom → reload chat URL (preserves id) → rescan INPUT_SELECTORS. Only dump if all fail. |
| **#55 wiki category mirror in universe_server** | `workflow/universe_server.py` _wiki_write handler + enum | #26 done | claimed:dev-2 (2026-04-13) | Mirror dev-3's 9-category expansion (add recipes, workflows, notes, references, plans) into Universe Server's duplicate wiki dispatcher. |
| **#4 docstring drift in workflow/universe_server.py** | `workflow/universe_server.py` docstring only | - | pending | Says `python -m workflow universe-server` but workflow/__main__.py has no such subcommand. Only console script invokes. |
| **#5 trim Universe Server tool docstrings for phone** | `workflow/universe_server.py` descriptions | - | pending | planner audit 2026-04-12: universe (~66 lines, 13 optional params), wiki (~58 lines, 14 params) have wall-of-text docstrings + duplicated action-table/Args narrative. Keep Args, kill duplicate table, replace ASCII tables with bullets. |
| **#33 rich-content capture in claude_chat.py** | `scripts/claude_chat.py` | - | in progress by dev (mostly landed; verify) | Captures claude.ai artifacts + inline mermaid to trace; current state mostly shipped. Verify end-to-end works and close. |
| **#54 Phase 5: Goal as first-class primitive** | new `workflow/goals.py` likely, `workflow/universe_server.py` new `goals` coarse tool | Phase 4 done | pending (spec-first) | Per PLAN.md Multi-User Evolutionary Design. `goals` table, `branches.goal_id` column, 5 actions: propose/list/get/bind/leaderboard + common_nodes. Gated on Phase 4 judgment data landing before leaderboards earn their keep. Planner scopes first. |
| **#56 Phase 6+: Outcome gates** | new schema, `workflow/universe_server.py` new `gates` coarse tool | #54 done | pending (spec-first) | Per PLAN.md "Outcome gates" subsection. gates table, gate_advances table, self-report actions, later automation for DOI/court-docket/sales/awards. Planner scopes after #54 ships. |
| **#31-under-Community-Branches Phase 4: original "social layer" label** | `workflow/universe_server.py` | - | superseded | Old Work-table entry "Phase 4: Social layer" was redefined 2026-04-13. The landing replaces social-layer semantics with within-branch iteration (judge/compare/rollback). Multi-user/social layer is now Phase 5+ (#54, #56). This row kept for trace. |

### Landed earlier (historical; do not reclaim)

See prior Work table entries below.

| Task | Files | Depends | Status | Verify |
|------|-------|---------|--------|--------|
| **Phase 2b: Full import migration + identity alignment** | all `.py` files, `pyproject.toml`, `.mcp.json`, `AGENTS.md`, `LAUNCH_PROMPT.md`, `.claude/agents/*.md`, `custom_gpt/`, docs | - | done | current: 2026-04-11 (Cowork session). 623 imports migrated across 46 files. 123 shim files built. 48 files resynced to workflow/domains. Entry points: `workflow-cli`, `workflow-mcp`, `workflow-universe-server`, `workflow` (GUI). Env: `WORKFLOW_UNIVERSE`. All agent defs + AGENTS.md + LAUNCH_PROMPT.md reframed to goal-agnostic daemon language. `fantasy_author/__main__.py` and `fantasy_author/api.py` kept as real code (Phase 5 bridge). Sandbox pytest: 3 collection errors (test_api.py import chain needs full deps, test_desktop.py needs GTK, test_gpt_harness.py needs playwright) — none are migration regressions. **Needs real-machine verification**: `pytest tests/ -q` with full Python 3.11+ deps. |
| **Initialize GitHub repo and push initial commit** | `.gitignore`, `LICENSE`, root | - | done | current: 2026-04-11 pushed to `https://github.com/Jonnyton/Workflow` (public, MIT). 484 files, 127K lines. Commit 36f097a. TODO: fix repo description typo ("multiplayr" → "multiplayer"). |
| **Unified structured artifacts initiative** (BettaFish + memory graph, user-approved 2026-04-10) | see sub-tasks | - | in progress | current: 2026-04-10 user approved merging BettaFish IR research and memory graph into one phased initiative. Critic's Sporemarch review validates urgency. |
| — 1a: Define scene commit packet schema | new `fantasy_author/packets.py`, `docs/specs/scene-packet.md` | - | done | current: 2026-04-11 schema defined as TypedDict (ScenePacket) with core identity, POV/setting, participants, facts, promises, world-state deltas, editorial summary, entry/exit state continuity hooks. Validation gate included. |
| — 1a: Emit scene packets from commit pipeline | `fantasy_author/nodes/commit.py` | schema | done | current: 2026-04-11 `_emit_scene_packet()` writes `.packet.json` alongside prose on accept. Reviewer-flagged bugs (participants type, enum serialization) fixed and re-approved. 3 tests. |
| — 1a: Packet validation gate in commit | `fantasy_author/packets.py` | schema | done | current: 2026-04-11 included in packet schema — validates pov_character, position, participants, entry/exit state non-empty. |
| — 1a: Packet query surface in orient | `fantasy_author/nodes/orient.py`, retrieval | emission | pending | |
| — 1b: Semantic clustering in retrieval | retrieval modules | - | done | current: 2026-04-11 already implemented in `fantasy_author/retrieval/router.py` (greedy single-pass, cosine + Jaccard fallback). Synced to `workflow/retrieval/router.py`. 19 tests pass. Reviewer approved. |
| — 1b: Bounded reflection in orient | `fantasy_author/nodes/orient.py` | - | done | current: 2026-04-11 3 new gap types added (premise_mismatch, continuity_gap, promise_context_gap). All deterministic, zero LLM. Promise key fix (text vs trigger_text) applied. 22 tests. Reviewer approved. |
| — 1b: json-repair + tenacity adoption | `requirements.txt`, providers | - | done | current: 2026-04-10 landed |
| **Fix universe isolation: CLI --db default + KG path** | `fantasy_author/__main__.py`, `fantasy_author/_paths.py`, root-level DBs | - | done | current: 2026-04-11 5 bugs fixed (CWD-relative defaults in KG, world_state_db, agentic_search, graph state propagation, archival memory). `_paths.py` helper added. Reviewer blockers resolved (is_relative_to, init_db guard). 20 tests, all passing. |
| **Add premise grounding to evaluator** | `fantasy_author/nodes/commit.py`, `fantasy_author/evaluation/structural.py`, `fantasy_author/nodes/orient.py` | - | done | current: 2026-04-11 deterministic premise_grounding check added to structural evaluator (protagonist presence, world-term grounding, cross-universe contamination). Hard failure on zero world terms or missing protagonist. Editorial reader now receives premise context. Reviewer approved, tester confirms no regressions. |
| **Community Branches: make the LangGraph community-designed** | `fantasy_author/universe_server.py`, `fantasy_author/author_server.py`, `fantasy_author/branches.py` | structured artifacts initiative | in progress | current: 2026-04-11 Phase 1 complete. Plan approved by user. Full plan doc at `Community_Branches_Plan.docx`. 5 phases: (1) schema+storage (**done**), (2) graph design + state design MCP tools, (3) generic graph runner, (4) social layer, (5) Fantasy Author migration. |
| — Phase 1: Schema and storage | `fantasy_author/branches.py`, `author_server.py`, `universe_server.py` | - | done | current: 2026-04-11 BranchDefinition + NodeDefinition + GraphNodeRef + EdgeDefinition + ConditionalEdge dataclasses. SQLite tables with CRUD. .node_registry.json migrated to SQLite (backward compat). Graph topology serialization with LangGraph-native JSON format. Validation: orphan detection, cycle-without-exit detection, entry point required. domain_id scoping. StateSchema deferred to Phase 3 (unvalidated JSON blob for now). 93 tests. Reviewer approved all 4 sub-tasks. |
| — Phase 2: Graph design + state design MCP tools | `universe_server.py` (create_branch, create_node, connect_nodes, add_state_field, etc.) | Phase 1 | pending | |
| — Phase 3: Generic graph runner | new compiler module, run/pause/resume/checkpoint tools | Phase 2 | pending | |
| — Phase 4: Social layer | browse/fork/remix/rate/lineage tools in Universe Server | Phase 3 | pending | |
| — Phase 5: Fantasy Author migration | re-express FA pipeline as BranchDefinition | Phase 3 | pending | |
| **Add wiki tools to Universe Server for remote access** | `fantasy_author/universe_server.py` | - | done | current: 2026-04-11 10 wiki actions implemented as FastMCP @mcp.tool: wiki_read, wiki_search, wiki_list, wiki_write (drafts gate), wiki_consolidate, wiki_promote (lint gate), wiki_ingest, wiki_lint, wiki_sync_projects. Draft gate enforced. Slug sanitization. Reviewer approved, no blockers. |
| Automate registry draft refresh from the built MCPB bundle | `packaging/registry/`, `docs/`, `STATUS.md` | Revalidate plugin and registry launch artifacts against current public formats | done | current: 2026-04-08 `packaging/registry/generate_server_json.py` now regenerates `packaging/registry/server.json` from the built MCPB bundle and current MCPB manifest version, including the computed SHA-256 and derived GitHub release URL. Verified with `python packaging/registry/generate_server_json.py --validate`, `python packaging/registry/generate_server_json.py --check --validate`, `python -m ruff check packaging/registry/generate_server_json.py`, and `python -m py_compile packaging/registry/generate_server_json.py`. |
| Revalidate plugin and registry launch artifacts against current public formats | `packaging/claude-plugin/`, `packaging/registry/`, `docs/`, `STATUS.md` | Package Universe Server as MCPB desktop extension | done | current: 2026-04-08 the Claude plugin marketplace was rebuilt into a valid `.claude-plugin/marketplace.json` plus nested plugin structure, and `claude plugin validate packaging/claude-plugin` and `claude plugin validate packaging/claude-plugin/plugins/workflow-universe-server` now both pass. The registry draft was rewritten to `packaging/registry/server.json` and now passes local validation against `https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json`. Remaining risk is publication of the referenced public `.mcpb` release asset, not schema shape. |
| Add MCP tool annotations and discoverability descriptions | `fantasy_author/universe_server.py`, `fantasy_author/mcp_server.py`, `pyproject.toml` | - | done | current: 2026-04-08 both MCP servers annotated with FastMCP 3.2.2 `ToolAnnotations` (readOnlyHint, destructiveHint, idempotentHint, openWorldHint), tags, and titles. `universe_server.py` tools have rich action-by-action descriptions with read/write grouping and usage hints. `mcp_server.py` all 12 tools annotated with correct read-only hints. Server instructions updated with orientation guidance. `pyproject.toml` updated with explicit MCP entry points (`fantasy-author-mcp`, `fantasy-author-universe-server`). AST-verified and annotations confirmed via `mcp.list_tools()` introspection. |
| Package Universe Server as MCPB desktop extension | `packaging/mcpb/`, `packaging/dist/`, `assets/icon.png`, `docs/mcpb_packaging.md`, `STATUS.md` | Add MCP tool annotations and discoverability descriptions | done | current: 2026-04-08 the MCPB bundle was rewritten against the current official spec, with a staged bundle template (`packaging/mcpb/manifest.json`, `server.py`, `pyproject.toml`, `build_bundle.py`) plus docs and a PNG icon. Verified on local Windows with `python packaging/mcpb/build_bundle.py --validate`, `python packaging/mcpb/build_bundle.py --pack`, `python -m ruff check packaging/mcpb/build_bundle.py packaging/mcpb/server.py`, and `python -m py_compile packaging/mcpb/build_bundle.py packaging/mcpb/server.py`. Official `@anthropic-ai/mcpb` validation passes, and `packaging/dist/workflow-universe-server.mcpb` was produced successfully. |
| Package Workflow / Universe Server for Claude Code and Cowork plugin marketplace | `packaging/claude-plugin/` | Add MCP tool annotations and discoverability descriptions | done | current: 2026-04-08 the marketplace now lives at `packaging/claude-plugin/.claude-plugin/marketplace.json`, with the actual plugin under `plugins/workflow-universe-server/`, validated by `claude plugin validate packaging/claude-plugin` and `claude plugin validate packaging/claude-plugin/plugins/workflow-universe-server`. The plugin bundles a Python bootstrapper plus staged `universe_server.py` copy so it can run from Claude's plugin cache without depending on `uv` being installed. |
| Prepare MCP registry listing metadata | `packaging/registry/` | Add MCP tool annotations and discoverability descriptions | done | current: 2026-04-08 `packaging/registry/server.json` now matches the official `server.json` schema, with reverse-DNS naming, schema-valid repository metadata, icon metadata, and an MCPB package reference carrying the local bundle SHA-256. Verified locally with `jsonschema` against `https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json`. Publication remains blocked until the referenced GitHub Releases asset exists publicly. |
| Prepare Conway-ready webhook trigger and panel metadata | `fantasy_author/api.py`, `packaging/conway/` | Add MCP tool annotations and discoverability descriptions | done | current: 2026-04-08 webhook endpoints added to `api.py` (`/webhook/register`, `/webhook/registrations`, `/webhook/{id}`, `/webhook/conway`). `packaging/conway/panel-metadata.json` created with speculative panel definitions, quick actions, and webhook event types. Conway format is speculative — metadata is structured to be trivially adaptable when spec ships. |
| Clarify long-term project direction with user | `STATUS.md`, `PLAN.md` | Produce project planning sweep and season lessons synthesis | done | current: user answers on 2026-04-08 established Workflow as the primary identity, fantasy as the first benchmark branch, multi-host daemon summoning as the destination, cleaner architecture as the first priority class, and self-evolving workflow/state design as the core success criterion. |
| Produce project planning sweep and season lessons synthesis | `docs/planning_sweep_2026-04-08.md`, `STATUS.md` | Rewrite stale `PLAN.md` sections from revalidation evidence | done | current: `docs/planning_sweep_2026-04-08.md` written on 2026-04-08. It synthesizes what stabilized this season, what was learned, what remains unsettled, and the priority frame for the next reset discussion. Follow-up on this document is explicitly paused for tonight at the user's request; keep it referenced, but not active. |
| Implement repo-local large-doc query tool and update agent guidance | `scripts/`, `tests/`, `AGENTS.md`, `STATUS.md` | Research Codex long-text truncation mitigations and propose repo-safe handling guidance | done | current: `scripts/docview.py` and `workflow/docview.py` landed on 2026-04-08 with tested Markdown headings/section extraction, bounded line/search queries, JSON pointer access, list previews, output-size guardrails, and Windows-safe output fallback. `AGENTS.md` now requires `docview.py` for large Markdown/text/JSON artifacts before any raw whole-file read. Verified with `python -m pytest tests/test_docview.py -q`, `python -m ruff check workflow/docview.py scripts/docview.py tests/test_docview.py`, and live `docview.py` queries against `PLAN.md` and `output/sporemarch/notes.json`. |
| Rewrite stale `PLAN.md` sections from revalidation evidence | `PLAN.md`, `docs/plan_revalidation.md`, `STATUS.md` | Revalidate highest-risk `PLAN.md` sections against implementation and runtime evidence | done | current: `PLAN.md` now distinguishes target state from current bridged implementation in `System Shape`, `Retrieval And Memory`, `API And MCP Interface`, `Live State Shape`, and `Workflow Extraction`, using the 2026-04-08 revalidation report as source evidence. |
| Research Codex long-text truncation mitigations and propose repo-safe handling guidance | `STATUS.md` | - | done | current: 2026-04-08 investigation checked OpenAI Codex docs plus the current `openai/codex` repo. Findings: `AGENTS.md` discovery is capped by `project_doc_max_bytes` (32 KiB default), tool output is still truncated before model consumption, and recent official issue reports document long local-file/tool-output truncation around `10 KiB` / `200 lines`. Local repo audit shows `PLAN.md` and generated artifacts are alrea
# Urgent Refactor Research

- 2026-04-09: External deep-dive research landed at `docs/bettafish-refactor-research-2026-04-09.md`.
- Priority: urgent.
- Purpose: use BettaFish as a refactor reference for raising Workflow's generation pipeline from lightweight prose generation to durable IR-first authoring, validation, provenance, and moderator-guided synthesis.
- Constraint: use the research as architectural guidance, not direct code import, because the source repository is GPL-2.0 and several implementation choices conflict with Workflow hard rules.

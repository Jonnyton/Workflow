# Auto-exported from Workflow Postgres. Do not hand-edit in this repo;
# submit changes via PR — they round-trip back into Postgres on merge.
license: CC0-1.0
schema_version: 1
branch_def_id: 22222222-2222-2222-2222-222222222201
slug: research-paper-pipeline
name: Research Paper Pipeline
domain: research-paper
status: published
structural_hash: 3f9a1e7b
version: 1
tags:
  - research
  - multi-step
  - benchmark
entry_point: literature_gap_mapper
concept: |
  {"purpose":
   "End-to-end pipeline: from topic + prior work to draftable hypotheses + method scaffold + citation bundle. One of the canonical stress-test branches per §15.5 of the full-platform design note.",
   "description":
   "5-node sequence with optional iteration at the hypothesis-generation step. Uses the research-hypothesis-generator node as the structural centerpiece.",
   "intended_users":
   "Academic researchers starting a new paper. Chatbot-driven design; runs on whichever daemon supplies the research-hypothesis-generator capability."}
nodes:
  - node_ref:
      source: standalone
      node_id: 11111111-1111-1111-1111-111111111102  # research-hypothesis-generator
    phase: analyze
    input_mapping: {topic: "state.topic", prior_work: "state.prior_work"}
    output_mapping: {hypotheses: "state.candidate_hypotheses"}
  # Other nodes here (literature_gap_mapper, methodology_selector, citation_formatter)
  # would be separately-registered nodes referenced by ID. v0 skeleton; real branch
  # populates once the full node library exists.
edges:
  - {from: START, to: literature_gap_mapper}
  - {from: literature_gap_mapper, to: research_hypothesis_generator}
  - {from: research_hypothesis_generator, to: methodology_selector}
  - {from: methodology_selector, to: citation_formatter}
  - {from: citation_formatter, to: END}
state_schema:
  - {field_name: topic, field_type: string, required: true}
  - {field_name: prior_work, field_type: array, item_type: string}
  - {field_name: candidate_hypotheses, field_type: array}
  - {field_name: selected_method, field_type: object}
  - {field_name: citation_bundle, field_type: object}

# Auto-exported from Workflow Postgres. Do not hand-edit in this repo;
# submit changes via PR — they round-trip back into Postgres on merge.
license: CC0-1.0
schema_version: 1
node_id: 00000000-0000-0000-0000-000000000001
slug: example-node
name: Example Node
domain: examples
status: published
structural_hash: abc123
version: 1
tags:
  - example
  - scaffold
parents: []
input_schema: |
  {"type": "object", "properties": {"text": {"type": "string"}}}
output_schema: |
  {"type": "object", "properties": {"word_count": {"type": "integer"}}}
concept: |
  {"purpose": "Count words in an input string.",
   "steps": [
     "Receive text via input_schema.text",
     "Split on whitespace",
     "Return count via output_schema.word_count"
   ],
   "notes": "Trivial scaffold example to demonstrate the catalog YAML shape."}

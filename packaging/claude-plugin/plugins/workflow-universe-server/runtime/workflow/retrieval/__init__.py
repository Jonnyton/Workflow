"""Retrieval layer -- agentic router, vector store, phase-aware context.

This package owns:
  - LanceDB vector store (singleton connection)
  - Agentic retrieval router (query decomposition + routing)
  - Phase-aware context assembly (orient/plan/draft/evaluate)
"""

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "workflow-universe-server-mcpb"
version = "0.1.0"
description = "Minimal uv runtime for the Workflow Universe Server MCP bundle"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.2,<4",
]

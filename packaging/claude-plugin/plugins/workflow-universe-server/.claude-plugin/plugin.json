{
  "name": "workflow-universe-server",
  "version": "0.1.0",
  "description": "Bundled Workflow Server runtime and explicit slash-style skills for inspecting and steering local universes.",
  "author": {
    "name": "Jonathan Farnsworth"
  },
  "homepage": "https://github.com/Jonnyton/Workflow",
  "repository": "https://github.com/Jonnyton/Workflow",
  "license": "MIT",
  "keywords": [
    "workflow",
    "mcp",
    "daemon",
    "fiction",
    "langgraph"
  ],
  "userConfig": {
    "universe_base": {
      "type": "directory",
      "title": "Universe Base Directory",
      "description": "Directory containing your Workflow universe folders, usually the repo output directory."
    },
    "default_universe": {
      "type": "string",
      "title": "Default Universe",
      "description": "Optional default universe ID when a command does not specify one.",
      "sensitive": false
    }
  },
  "mcpServers": {
    "workflow-universe-server": {
      "command": "python",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/runtime/bootstrap.py"
      ],
      "env": {
        "UNIVERSE_SERVER_BASE": "${user_config.universe_base}",
        "UNIVERSE_SERVER_DEFAULT_UNIVERSE": "${user_config.default_universe}"
      }
    }
  }
}
